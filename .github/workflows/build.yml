name: Build and Test

on:
  workflow_call:
    inputs:
      archive-name:
        required: false
        type: string
        default: "${{ github.sha }}-${{ github.run_id }}"
      java:
        required: false
        type: string
        default: 11
      maven-flags:
        required: false
        type: string
        default: ''
      optional:
        required: false
        type: boolean
        default: false
      os:
        required: false
        type: string
        default: ubuntu-latest
      tests:
        required: true
        type: boolean
    secrets:
      DEVELOCITY_ACCESS_KEY:
        required: false
      GH_TOKEN:
        required: false

env:
  GITHUB_OAUTH: ${{ secrets.GH_TOKEN }}
  DEVELOCITY_ACCESS_KEY: ${{ secrets.DEVELOCITY_ACCESS_KEY }}

jobs:
  Build:
    runs-on: ${{ inputs.os }}
    continue-on-error: ${{ inputs.optional }}
    name: Build
    outputs:
      maven-cache-key: ${{ steps.maven-cache-key.outputs.key }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate cache key
        id: maven-cache-key
        run: |
          echo "key=cache-${{ github.run_number }}" >> $GITHUB_OUTPUT
        shell: bash

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: ${{ inputs.java }}
          distribution: 'zulu'
          server-id: central
          cache: maven

      - name: Find maven
        id: maven
        shell: bash
        run: echo "maven=$( test -f mvnw  && echo "./mvnw" || echo mvn )" >> $GITHUB_OUTPUT

      - name: Restore cached build artifacts
        if: ${{ inputs.tests }}
        uses: actions/cache@v4
        with:
          path: |
            ~/.m2/repository
          key: target-${{ steps.maven-cache-key.outputs.key }}
          restore-keys: target-${{ steps.maven-cache-key.outputs.key }}
          fail-on-cache-miss: false

      - name: Build with Tests
        if: ${{ inputs.tests }}
        run: ${{ steps.maven.outputs.maven }} -U clean install ${{ inputs.maven-flags }}

      - name: Build without Tests
        if: ${{ !inputs.tests }}
        run: ${{ steps.maven.outputs.maven }} -U clean install -DskipTests ${{ inputs.maven-flags }}

      - name: Cache build artifacts
        if: ${{ !inputs.tests }}
        uses: actions/cache@v4
        with:
          path: |
            ~/.m2/repository
          key: target-${{ steps.maven-cache-key.outputs.key }}
          restore-keys: target-${{ steps.maven-cache-key.outputs.key }}

      - name: collect logs
        uses: actions/upload-artifact@v4
        if: failure() && inputs.archive-name != ''
        with:
          name: "logs-${{ inputs.archive-name }}"
          path: |
            **/target/**/*.log
            **/target/**/*.err
          retention-days: 7
